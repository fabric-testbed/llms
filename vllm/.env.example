# HuggingFace Authentication
# Required for downloading gated models (e.g., Llama, GPT-OSS)
# Get your token from: https://huggingface.co/settings/tokens
HF_TOKEN=your_huggingface_token_here

# NGINX Port Configuration
# HTTPS port (default: 443)
NGINX_HTTPS_PORT=443

# HTTP port (default: 80, redirects to HTTPS)
NGINX_HTTP_PORT=80

# vLLM Logging Level
# Options: DEBUG, INFO, WARNING, ERROR
VLLM_LOGGING_LEVEL=INFO

# Optional: Override default HuggingFace cache location
# Default: ~/.cache/huggingface
# HF_HOME=/path/to/custom/cache

# ============================================
# LiteLLM Proxy Configuration
# ============================================

# LiteLLM Master API Key (required for authentication)
# This key is used to access the LiteLLM proxy API
LITELLM_MASTER_KEY=sk-1234-change-this-to-a-secure-key

# LiteLLM UI Admin Password
LITELLM_UI_PASSWORD=admin

# LiteLLM Port (default: 4000)
LITELLM_PORT=4000

# Redis Port (default: 6379)
LITELLM_REDIS_PORT=6379

# PostgreSQL Port (default: 5432)
LITELLM_POSTGRES_PORT=5432

# PostgreSQL Password
LITELLM_POSTGRES_PASSWORD=litellm
